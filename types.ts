export type TokenGeneratedType = {
  access_token: string;
  client_id: string;
  token_type: string;
  expires_in: number;
  refresh_token: string;
  ibs_client_id: string;
  ibs_user_id: string;
};
